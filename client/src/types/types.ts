
export type MenuItem = {
    label: string,
    value: string,
}

export type Definition = {
    id: number, 
    title: string, 
    description: string,
}

export type StringDict = {
    [key:string] : string,
}